import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  avatarUrl: {
    height: 34,
    width: 34,
    borderRadius: 17,
    marginRight: 10,
  },
  username: {
    fontSize: 18,
    fontWeight: 'bold',
  }
});