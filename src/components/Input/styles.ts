import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 50,
    width: '100%',
    backgroundColor: 'lightgray',
    borderRadius: 10,
    fontSize: 16,
    paddingLeft: 10,
  }
});